#include<stdio.h>
void main()
{
	char s[20],ch;
	printf("Enter string : ");
	scanf("%[^\n]",s);
	printf("Enter char to delete : ");
	scanf(" %c",&ch);
	
	int len;
	for(len=0;s[len];len++);
	
	while(a[len])
	{
		if(a[len-1]==ch)
		{
			while(a[i])
			
		}
	}
}
